CREATE TABLE champ (ID INTEGER IDENTITY NOT NULL, c_name VARCHAR, PRIMARY KEY (ID))
CREATE TABLE typ (ID INTEGER IDENTITY NOT NULL, t_name VARCHAR, PRIMARY KEY (ID))
CREATE TABLE tagging (ID INTEGER IDENTITY NOT NULL, champ_fs INTEGER, typ_fs INTEGER, PRIMARY KEY (ID))
ALTER TABLE tagging ADD CONSTRAINT FK_tagging_champ_fs FOREIGN KEY (champ_fs) REFERENCES champ (ID)
ALTER TABLE tagging ADD CONSTRAINT FK_tagging_typ_fs FOREIGN KEY (typ_fs) REFERENCES typ (ID)
